Скелет скрипта (флаги):
https://jenyay.net/Programming/Argparse#noargparse - про Примеры без использования argparse

что должна уметь прога?

я предполагаю, что скрипт проги лежит в одной папке с fastq файлом

1. взять файл с исходными ридами (аргумент с именем файла передается в конце всей команды)
  1.1 программа должна взять именно последний введенный аргумент (в нем лежит имя файла)
  1.2 программа должна знать путь до папки в которой она сама лежит (положить этот путь в переменную строковую dir_with_script)
  1.3 программа должна сложть инфу из последнего введенного аргумента с именем файла из fastq к пути из dir_with_script в новую переменную dir_with_file 

2. программа должна начать считывать построчно* рииды из файла с ридами, опираясь на путь до этого файла, который уже лежит в dir_with_script
  2.1 построчно* - начиная со второй строки через пять строчек надо прыгать при считывании
  
3. Надо чтобы программа сожрав fastq файл делала так:
  суперпсевдо код: 
                  3.1  if --output_base_name <string> in sys.argv:
                          то в переменную string_new_name кладем <string>
                       else:
                          то в переменную string_new_name кладем последний введенный аргумент
                  
                  3.2  if --min_length <int> in sys.argv:
                          то кладем в переменную int_min_len значение <int>
                          if --keep_filtered in sys.argv:
                              то копируем риды (все четыре строчки!) длинной менее int_min_len в файл с именем из string_new_name в путь dir_with_script,
                          создаем новый файл в пути dir_with_script и с имнем из переменной string_new_name
